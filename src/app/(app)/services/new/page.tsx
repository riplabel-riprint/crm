import { ServiceCreatorScreen } from "@/components/services/ServiceCreatorScreen";

export const metadata = { title: "Nowa usługa | Riprint" };

export default function NewServicePage() {
  return <ServiceCreatorScreen />;
}
